:ruby
  semver = '3.0.0.pre0' # TODO: semver
  footer = {
    copy: {
      logo: {
        href: 'http://madek.zhdk.ch',
        # content: component('icon.madek', 'ui-footer-logo')
        content: "Madek"
      },
      text: "#{semver}. Zürcher Hochschule der Künste and contributors"
    }
  }

- content_for(:body) do
  %body

    #app.app{data: { view: request[:controller], action: request[:action] }}

      = render('application/app_header')

      = render('application/app_alerts')

      = yield

      .app-footer-push
  = combo('footer', footer)
  -# = javascript_include_tag 'madek-ujs' # init

= render template: 'layouts/_base'
